@model HVAC.Models.EstimationSearch

@{
    ViewBag.Title = "List";
    ViewBag.pTitle = "Estimation";
    ViewBag.pageTitle = "HVAC";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<script type="text/javascript">

    $(document).ready(function () {
        $('#btnOpenCreate').click(function () {
            if ($('#EnquiryNo').val() != '') {
                window.location.href = "/Estimation/Create?id=0&EnquiryNo=" + $('#EnquiryNo').val();
            }
            else {
                Swal.fire("Data Validation", "Select Enquiry No.", "Info");
            }
        })
        $('#dataTables-example tfoot th').each(function () {
            var title = $(this).text();
            $(this).html('<input type="text" style="width:100%" />');
        });


        var table = $('#dataTables-example').DataTable();

        table.columns().every(function () {
            var that = this;

            $('input', this.footer()).on('keyup change', function () {
                if (that.search() !== this.value) {
                    that
                        .search(this.value)
                        .draw();
                }
            });
        });

    });

   
</script>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="card">
        <div class="card-body">
            <div class="row">


                <div class="col-md-2">
                    <label for="FromDate" class="col-form-label">From Date</label>
                    @*@Html.TextBoxFor(mode => mode.FromDate, new { @class = "form-control text-right color_blue_border" })*@
                    <div class="docs-datepicker">
                        <div class="input-group">
                            <input type="text" class="form-control docs-date" name="FromDate" id="FromDate1"
                                   placeholder="Pick a From Date" autocomplete="off" value="@Model.FromDate.ToShortDateString()" />
                            <button type="button"
                                    class="btn btn-secondary docs-datepicker-trigger" )
                                    disabled>
                                <i class="mdi mdi-calendar" aria-hidden="true"></i>
                            </button>
                        </div>
                        <div class="docs-datepicker-container"></div>
                    </div>
                </div>

                <div class="col-md-2">
                    <label for="ToDate" class="col-form-label">To Date</label>
                    @*@Html.TextBoxFor(mode => mode.ToDate, new { @class = "form-control text-right color_blue_border" })*@
                    <div class="docs-datepicker">
                        <div class="input-group">
                            <input type="text" class="form-control docs-date" name="ToDate" id="ToDate1" data-date-format="dd/MM/yyyy" data-date-autoclose="true" data-provide="datepicker"
                                   placeholder="Pick a To date" autocomplete="off" value="@Model.ToDate.ToShortDateString()">
                            <button type="button"
                                    class="btn btn-secondary docs-datepicker-trigger"
                                    disabled>
                                <i class="mdi mdi-calendar" aria-hidden="true"></i>
                            </button>
                        </div>
                        <div class="docs-datepicker-container"></div>
                    </div>
                </div>
                <div class=" col-md-2">
                    <div class="mb-2">
                        <label for="Receipt No." class="col-form-label">Enquiry No.</label>
                        @Html.TextBoxFor(model => model.EnquiryNo, new { @class = "form-control" })
                    </div>
                </div>

                <div class=" col-md-2">
                    <div class="mb-2">
                        <label for="Receipt No." class="col-form-label">Estimation No.</label>
                        @Html.TextBoxFor(model => model.EstimationNo, new { @class = "form-control" })
                    </div>
                </div>
                <div class="col-md-1">
                    <div class="mb-2">
                        <button type="submit" class="btn btn-sm btn-success" style="margin-top:35px">
                            <i class="mdi mdi-refresh" style="font-size: 18px;"></i>
                        </button>
                    </div>
                </div>
                <div class="col-md-3 text-md-end">
                    <a class="btn btn-primary waves-effect waves-light text-md-end btn_top" id="btnOpenCreate"   title="Create Estimation on Selected Enquiry"  href="javascript:void(0)"><i class="mdi mdi-plus me-1"></i> New Estimation</a>
                </div>
            </div>
        </div>
    </div>

    <div class="card">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-bordered mb-0" id="datatable" style="width:100%">
                    <thead class="table-light">
                        <tr>
                            <th>
                               Estimation Date

                            </th>
                            <th>Estimation No.</th>
                            <th>Version No.</th>
                            <th>Enquiry No.</th>
                            <th width="20%">
                                Project Details
                            </th>
                            <th>
                                Created By
                            </th>
                            <th>
                                Total Landing  Cost (OMR)
                            </th>
                            <th>
                                Selling Value
                            </th>
                            <th></th>

                            <th>Action</th>
                        </tr>
                    </thead>


                    <tbody>
                        @foreach (var item in Model.Details)
                        {

                            <tr>


                                <td>
                                    @HVAC.Models.CommonFunctions.GetShortDateFormat(item.EstimationDate)

                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.EstimationNo)
                                    @*@{
                                        if (@item.PONo != "")
                                        {
                                            <br />
                                            <div class="badge bg-primary rounded">
                                                @item.PONo

                                            </div>
                                        }
                                    }*@
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.VarNo)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.EnquiryNo)
                                </td>
                                <td>
                                    <span class="text-over colorblue" style="width:120px">@item.ProjectName</span>                                    
                                </td>

                                <td>
                                    @Html.DisplayFor(modelItem => item.EmployeeName)
                                </td>
                                <td style="text-align:right">
                                    @HVAC.Models.CommonFunctions.GetFormatNumber(item.TotalLandingCostOMR,"3")
                                </td>
                                <td style="text-align:right">
                                    @HVAC.Models.CommonFunctions.GetFormatNumber(item.SellingValue, "3")
                                    
                                </td>
                                    <td>

                                        <div class="dropdown">
                                            <a href = "#" class="dropdown-toggle card-drop" data-bs-toggle="dropdown" aria-expanded="true">
                                                <i class="mdi mdi-dots-horizontal font-size-18" title="Sub Menus"></i>
                                            </a>

                                            <ul class="dropdown-menu dropdown-menu-end" style="position: absolute; inset: 0px 0px auto auto; margin: 0px; transform: translate3d(-84px, 27px, 0px);" data-popper-placement="bottom-end">
                                                @*<li><a href="/Quotation/Create?id=0&EnquiryID=@item.EnquiryID" class="dropdown-item remove-list"><i class="mdi mdi-vector-intersection font-size-16 text-danger me-1"></i> Quotation</a></li>*@
                                                <li><a href="/Enquiry/GotoQuotation?EnquiryNo=@item.EnquiryNo" class="dropdown-item edit-list" data-edit-id="7"><i class="mdi mdi-file-account font-size-16 text-success me-1"></i> Quotation</a></li>
                                            </ul>
                                        </div>
                                @*<div class="dropdown-menu dropdown-menu-end">

        <a class="dropdown-item" href="/Enquiry/Quotation?id=@item.EnquiryID">Quotation</a>


    </div>*@

                                </td>
                                <td>
                                    <div class="d-flex gap-3">
                                        <a href="@Url.Action("Create", new { id = item.EstimationID })" class="text-success" title="Edit Estimation"><i class="mdi mdi-pencil font-size-18"></i></a>
                                        <a href="@Url.Action("Create", new { id = 0,EnquiryID=0,EstimationId=item.EstimationID })" class="text-primary" title="New Revision"><i class="mdi mdi-repeat font-size-18"></i></a>
                                        <a href="#" onclick="Delete(ID =@item.EstimationID)" class="text-danger" title="Delete Estimation"><i class="mdi mdi-delete font-size-18"></i></a>

                                        <a href="/Estimation/ReportPrint?id=@item.EstimationID" class="text-warning" title="Print Estimation"><i class="mdi mdi-printer font-size-18"></i></a>
                                        
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>

                </table>
            </div>
        </div>
    </div>
}
<script type="text/javascript">


    function Delete(ID) {
        Swal.fire({ title: "Are you sure?", text: "Do you want to delete this?", icon: "warning", showCancelButton: !0, confirmButtonColor: "#34c38f", cancelButtonColor: "#f46a6a", confirmButtonText: "Yes, delete it!" }).then(
            function (t) {
                if (t.value) {
                    $.ajax({
                        type: "POST",
                        url: '/Estimation/DeleteEstimation',
                        datatype: "html",
                        data: {
                            'id': ID
                        },
                        success: function (response) {
                            if (response.status == "OK") {
                                Swal.fire("Delete Status!", response.message, "success");
                                window.location.reload();
                            }
                            else {
                                Swal.fire("Delete Status!", response.message, "error");
                            }
                        }
                    });
                }
            });

    }
</script>
